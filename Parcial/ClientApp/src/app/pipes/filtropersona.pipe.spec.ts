import { FiltropersonaPipe } from './filtropersona.pipe';

describe('FiltropersonaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltropersonaPipe();
    expect(pipe).toBeTruthy();
  });
});
