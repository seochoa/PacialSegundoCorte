import { FiltrovacunaPipe } from './filtrovacuna.pipe';

describe('FiltrovacunaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrovacunaPipe();
    expect(pipe).toBeTruthy();
  });
});
